name: Pull Latest Code

on:
  push:
    branches:
      - main

jobs:
  pull-code:
    runs-on: self-hosted
    steps:
      - name: Git Fetch and Pull
        run: |
          cd /home/cse438/trendyol-clone/repos/Web
          git fetch origin
          git checkout main
          git pull origin main
